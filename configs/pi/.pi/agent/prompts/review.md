---
description: Review the current git diff (or named files) with the slow reviewer.
---
Use the subagent tool with agent "reviewer". Task: review $@ (default: the current uncommitted diff). Relay the Critical and Warnings sections verbatim; summarise the rest.
