# This file is NOT used - os.toml is generated dynamically by variants/osx/setup.sh
# to inject the absolute herdr path (execve does not search PATH)
# See variants/osx/setup.sh for the actual configuration
