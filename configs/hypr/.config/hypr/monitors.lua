-- Monitor layout and scale are managed by Omarchy's Display UI.
-- List current monitors and supported resolutions with: hyprctl monitors all

hl.monitor({ output = "", mode = "preferred", position = "auto", scale = 1.25 })
